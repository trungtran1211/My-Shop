import React from 'react';
import HeaderTop from './Header-wrapper/HeaderTop'

const Header = () => {
    return (
        <header>
            <HeaderTop/>
        </header>
    );
};

export default Header;